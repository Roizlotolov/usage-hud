{
  "description": "No cost/quota data available (e.g. no pricing table configured, provider not supported) - must degrade gracefully, no errors, no zeros",
  "snapshot": {
    "model": "claude-opus-4-8",
    "tokens": { "input": 1200, "output": 340 },
    "context": { "usedTokens": 41000, "limitTokens": 100000 }
  },
  "config": {
    "footer": { "enabled": true, "fields": ["model", "tokens", "context", "cost", "quota"] }
  }
}
