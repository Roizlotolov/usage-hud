{
  "description": "quota window remainingPct (15, derived from usedPct 85) <= thresholdPct (20) -> budget alert fires",
  "snapshot": {
    "model": "claude-opus-4-8",
    "quota": { "windows": [{ "label": "Claude weekly", "usedPct": 85 }] },
    "route": { "sessionKey": "s1" }
  },
  "config": {
    "alerts": { "budget": { "enabled": true, "thresholdPct": 20 } }
  }
}
